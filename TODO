Database (refactor)
users:
  - username (unique, not null,)
  - password
  - birthday
  - sex (female, male, other) -> will not add 'other' to the all statistics
  - role ENUM('admin', 'role') -> dont need have more than 2 roles so dont have to create extra table
all other tables: 
  have to change join from 'email' to username

WebPage (refactor)
  - change register endpoint and front
  - change login endpoint and front

BackEnd
  - change queries in all endpoints

Application
  - change login endpoint and front in app
